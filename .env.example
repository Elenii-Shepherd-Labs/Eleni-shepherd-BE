# Eleni Shepherd - Environment Variables
# Copy to .env and fill in values

# App
PORT=3000
NODE_ENV=development

# Database (MongoDB)
DATABASE_URL=mongodb://localhost:27017/eleni-shepherd

# Redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# Session
SESSION_SECRET=your-session-secret

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback
# Optional: override the post-auth redirect URL. If your client (web/mobile)
# needs a custom deep link (e.g. elenii://Onboarding) or uses Expo AuthSession,
# pass the same URL as '?redirectUrl=…' on the /auth/google request or set this env var.
# Leave empty to allow client to specify via query parameter (recommended for mobile).
SUCCESS_REDIRECT_URL=

# OpenAI (Speech-to-Text, Text-to-Speech, LLM)
OPENAI_API_KEY=

# Anthropic (LLM alternative)
ANTHROPIC_API_KEY=

# Vision Service (Python YOLO microservice)
VISION_SERVICE_URL=http://localhost:5000

# Radio Browser (optional override)
RADIO_BROWSER_BASE_URL=https://de1.api.radio-browser.info

# GNews API (optional - for blog/news; without key uses Hacker News)
GNEWS_API_KEY=
